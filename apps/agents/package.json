{
  "name": "@ironcage/agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.check.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.check.json && tsc --noEmit -p tests"
  },
  "dependencies": {
    "@ironcage/contracts": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@effect/vitest": "catalog:",
    "@ironcage/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
