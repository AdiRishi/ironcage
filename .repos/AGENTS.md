# Vendored reference repositories

Everything below `.repos/` is read-only reference material. Do not import it,
edit it, format it, or include it in workspace package discovery.

The Effect subtree matches the `effect` version in `pnpm-workspace.yaml`. Run
`pnpm sync:repos` from a clean working tree after changing that pin.
