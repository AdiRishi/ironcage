# Direct PlanetScale branch connection used only by the migration runner.
# Local development points this at dev. Production supplies its own value to
# the release workflow; never put a production credential in this file.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/postgres?sslmode=verify-full"
